div#answer_form_container
{
	display: none
}
.odd{
	background-color:inherit;
}
.even{
	background-color:#DFDFD5;
}

.newest_questions{
	width:50%;
	height:auto;
	float:left;
}
	.newest_questions a{
		text-decoration:none;
	}

	.newest_questions .box{
		width:100%;
		height:auto;
		background:#FFF;
		margin:40px auto;
	}

	.newest_questions table{
		width:100%;
	}
	.newest_questions table tr,.newest_questions table tr td{
		width:100%;
		padding: 5px 15px;
	}


	.newest_questions .shadow_effect{
			position:relative;
			-webkit-box-shadow:0 1px 4px rgba(0, 0, 0, 0.3), 0 0 40px rgba(0, 0, 0, 0.1) inset;
				-moz-box-shadow:0 1px 4px rgba(0, 0, 0, 0.3), 0 0 40px rgba(0, 0, 0, 0.1) inset;
							box-shadow:0 1px 4px rgba(0, 0, 0, 0.3), 0 0 40px rgba(0, 0, 0, 0.1) inset;
	}
	.newest_questions .shadow_effect:before,.newest_questions .shadow_effect:after{
		content:"";
			position:absolute;
			z-index:-1;
			-webkit-box-shadow:0 0 20px rgba(0,0,0,0.8);
			-moz-box-shadow:0 0 20px rgba(0,0,0,0.8);
			box-shadow:0 0 20px rgba(0,0,0,0.8);
			top:10px;
			bottom:10px;
			left:0;
			right:0;
			-moz-border-radius:100px / 10px;
			border-radius:100px / 10px;
	}
	.newest_questions .shadow_effect:after{
		right:10px;
			left:auto;
			-webkit-transform:skew(8deg) rotate(3deg);
				-moz-transform:skew(8deg) rotate(3deg);
					-ms-transform:skew(8deg) rotate(3deg);
					-o-transform:skew(8deg) rotate(3deg);
							transform:skew(8deg) rotate(3deg);
	}

.semi_questions{
	width:40%;
	height:auto;
	margin-bottom:10px;
	float:right;
}
